namespace GSoftware.AspNetCore.ReCaptcha.TagHelpers
{
    /// <summary>
    /// Recaptcha badge options for the <see cref="RecaptchaTagHelper"/>. Only usable in combination
    /// with the <see cref="Size.Invisible"/> size option.
    /// </summary>
    public enum BadgePosition
    {
        BottomLeft,
        BottomRight,
        Inline
    }
}
